/**service part */
VanillaTilt.init(document.querySelectorAll(".service-item"), {
    max: 25,
    speed: 400,
glare: true,
"max-glare": 1,
});

/**testimonals part*/
VanillaTilt.init(document.querySelectorAll(".testimonials-item"), {
    max: 25,
    speed: 400,
glare: false,
"max-glare": 1,
});

/**portfoilio part*/
VanillaTilt.init(document.querySelectorAll(".project-img"), {
    max: 25,
    speed: 400,
glare: false,
"max-glare": 1,
});